# デザイントークン 使用方法ガイド

**Hidearea Design System Tokens**
**最終更新:** 2025-12-10
**対象読者:** フロントエンド開発者、UIデザイナー

---

## はじめに

このガイドでは、Hidearea Design System Tokensを実際のプロジェクトで使用する方法を説明します。基本的な使い方から、高度なカスタマイズまで、実践的な例を交えて解説します。

### 前提知識

- CSS Custom Properties (CSS変数) の基本
- HTML/CSSの基礎知識
- npmまたはpnpmの使用経験

---

## インストール

### パッケージのインストール

```bash
# pnpmを使用
pnpm add @hidearea-design/tokens

# npmを使用
npm install @hidearea-design/tokens

# yarnを使用
yarn add @hidearea-design/tokens
```

### ビルド済みファイルの使用

```html
<!DOCTYPE html>
<html data-theme="light">
<head>
  <!-- CSS変数を読み込み -->
  <link rel="stylesheet" href="node_modules/@hidearea-design/tokens/build/css/variables.css">
</head>
<body>
  <!-- コンテンツ -->
</body>
</html>
```

---

## ビルドパターン

このパッケージは**4つの異なるビルドパターン**を提供しており、プロジェクトのニーズに合わせて選択できます。

### Pattern 1: WebComponents (Shadow DOM)

**対象:** Web Components、Shadow DOMを使用するプロジェクト

```css
/* :host セレクタを使用 */
@import '@hidearea-design/tokens/css/components/layout/button.css';
```

**特徴:**
- `:host` セレクタによるスコープ化
- Shadow DOM内で使用
- 各コンポーネントが独立したスタイルを持つ

### Pattern 2: Plain HTML (クラスセレクタ)

**対象:** シンプルなHTML/CSSプロジェクト、プロトタイピング

```css
/* .ha-* プレフィックス付きクラス */
@import '@hidearea-design/tokens/css/html/layout/button.css';
```

```html
<!-- HTML使用例 -->
<div class="ha-button" variant="primary">
  <button>送信</button>
</div>
```

**特徴:**
- `.ha-*` プレフィックスでグローバルな名前空間を回避
- Shadow DOM不要
- 従来のHTML/CSSワークフローで使用可能

### Pattern 3: React/Vue (JavaScript/TypeScript)

**対象:** React、Vue、その他のJavaScriptフレームワーク

```javascript
// WebComponents版（:host セレクタ）
import { buttonStyles } from '@hidearea-design/tokens/styles/button';

// HTML版（.ha-* クラスセレクタ）
import { buttonHtmlStyles } from '@hidearea-design/tokens/styles/button';

// CSS-in-JSとして使用
const Button = styled.div`
  ${buttonHtmlStyles}
`;
```

**特徴:**
- JavaScriptモジュールとしてインポート
- CSS-in-JSライブラリと統合可能
- Tree-shaking対応で必要な分だけバンドル

### Pattern 4: 統合CSS (All-in-One)

**対象:** プロトタイピング、小〜中規模プロジェクト、シンプルな構成を好む場合

```css
/* WebComponents版（147KB） */
@import '@hidearea-design/tokens/css/all.css';

/* HTML版（149KB） - 推奨 */
@import '@hidearea-design/tokens/css/html/all.css';
```

**特徴:**
- 全38コンポーネントが1ファイルに統合
- HTTPリクエスト数を最小化
- プロトタイピングに最適
- 実例: [examples/unified/](../examples/unified/)

---

## 基本的な使い方

### 1. CSSでの使用（Pattern 2推奨）

最も基本的な使い方は、HTML版のCSSを使用することです。

```html
<!DOCTYPE html>
<html data-theme="light">
<head>
  <!-- デザイントークン変数 -->
  <link rel="stylesheet" href="node_modules/@hidearea-design/tokens/build/css/variables.css">

  <!-- 統合CSS（全コンポーネント） -->
  <link rel="stylesheet" href="node_modules/@hidearea-design/tokens/build/css/html/all.css">
</head>
<body>
  <!-- .ha-* クラスを使用 -->
  <div class="ha-button" variant="primary">
    <button>送信</button>
  </div>

  <div class="ha-input" size="md">
    <div class="input-wrapper">
      <input type="text" placeholder="名前を入力">
    </div>
  </div>
</body>
</html>
```

### 2. CSS変数での カスタムスタイリング

```css
/* カスタムボタンを作成 */
.my-custom-button {
  /* デザイントークンを使用 */
  background-color: var(--primary-default);
  color: var(--foreground-inverse);
  padding: var(--spacing-3) var(--spacing-6);
  border-radius: var(--border-radius-md);
  border: none;
  cursor: pointer;

  /* トランジション */
  transition: background-color var(--animation-duration-base);
}

.my-custom-button:hover {
  background-color: var(--primary-hover);
}

.my-custom-button:disabled {
  background-color: var(--background-disabled);
  color: var(--foreground-disabled);
  cursor: not-allowed;
}
```

### 2. JavaScriptでの使用

JavaScriptから直接トークンにアクセスすることもできます。

```javascript
import tokens from '@hidearea-design/tokens';

// トークン値を取得
const primaryColor = tokens.color.blue['500'];
console.log(primaryColor); // "#3b82f6"

// スペーシング値を取得
const spacing4 = tokens.spacing['4'];
console.log(spacing4); // "1rem"

// 動的にスタイルを適用
element.style.backgroundColor = tokens.component.button.primary.background.default;
```

### 3. TypeScriptでの使用

TypeScript環境では、自動生成された型定義を活用できます。

```typescript
import tokens from '@hidearea-design/tokens';
import type { DesignTokens } from '@hidearea-design/tokens';

// 型安全なトークンアクセス
const primaryBg: string = tokens.component.button.primary.background.default;

// オートコンプリートが効く
const spacing: string = tokens.spacing['4']; // IDE が候補を表示
```

---

## テーマの切り替え

### 基本的なテーマ切り替え

HTMLの `data-theme` 属性を変更するだけで、アプリケーション全体のテーマが切り替わります。

```javascript
// テーマ切り替え関数
function setTheme(theme) {
  document.documentElement.setAttribute('data-theme', theme);
  localStorage.setItem('theme', theme);
}

// ライトテーマに切り替え
setTheme('light');

// ダークテーマに切り替え
setTheme('dark');
```

### システム設定に従う

ユーザーのOSやブラウザの設定を尊重することも重要です。

```javascript
// システムのカラースキーム設定を検出
function getSystemTheme() {
  const prefersDark = window.matchMedia('(prefers-color-scheme: dark)');
  return prefersDark.matches ? 'dark' : 'light';
}

// 初回読み込み時にテーマを設定
function initializeTheme() {
  // 保存されたテーマを優先、なければシステム設定
  const savedTheme = localStorage.getItem('theme');
  const theme = savedTheme || getSystemTheme();
  setTheme(theme);
}

// システム設定の変更を監視
window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
  // ユーザーが明示的にテーマを選択していない場合のみ
  if (!localStorage.getItem('theme')) {
    setTheme(e.matches ? 'dark' : 'light');
  }
});

// ページ読み込み時に初期化
initializeTheme();
```

### Reactでのテーマ切り替え

```tsx
import React, { createContext, useContext, useEffect, useState } from 'react';

type Theme = 'light' | 'dark';

const ThemeContext = createContext<{
  theme: Theme;
  setTheme: (theme: Theme) => void;
}>({
  theme: 'light',
  setTheme: () => {},
});

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setThemeState] = useState<Theme>('light');

  useEffect(() => {
    // 保存されたテーマを読み込み
    const savedTheme = localStorage.getItem('theme') as Theme;
    if (savedTheme) {
      setThemeState(savedTheme);
    } else {
      // システム設定を使用
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)');
      setThemeState(prefersDark.matches ? 'dark' : 'light');
    }
  }, []);

  useEffect(() => {
    // テーマを適用
    document.documentElement.setAttribute('data-theme', theme);
    localStorage.setItem('theme', theme);
  }, [theme]);

  const setTheme = (newTheme: Theme) => {
    setThemeState(newTheme);
  };

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

export const useTheme = () => useContext(ThemeContext);

// 使用例
function ThemeToggle() {
  const { theme, setTheme } = useTheme();

  return (
    <button onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}>
      {theme === 'light' ? '🌙' : '☀️'}
    </button>
  );
}
```

---

## コンポーネントのスタイリング

### ボタンコンポーネント

```html
<!-- HTML -->
<button class="btn btn-primary">Primary Button</button>
<button class="btn btn-secondary">Secondary Button</button>
<button class="btn btn-ghost">Ghost Button</button>
<button class="btn btn-danger">Danger Button</button>
```

```css
/* CSS */
.btn {
  /* 基本スタイル */
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-2);

  /* タイポグラフィ */
  font-family: var(--font-family-sans);
  font-size: var(--font-size-base);
  font-weight: var(--font-weight-medium);
  line-height: var(--font-line-height-none);

  /* スペーシング */
  padding: var(--spacing-2-5) var(--spacing-4);

  /* ボーダー */
  border-width: var(--border-width-1);
  border-style: solid;
  border-radius: var(--border-radius-md);

  /* トランジション */
  transition-property: background-color, border-color, color, box-shadow;
  transition-duration: var(--animation-duration-fast);
  transition-timing-function: var(--animation-easing-ease);

  /* その他 */
  cursor: pointer;
  user-select: none;
}

/* プライマリボタン */
.btn-primary {
  background-color: var(--component-button-primary-background-default);
  color: var(--component-button-primary-text-default);
  border-color: var(--component-button-primary-border-default);
}

.btn-primary:hover:not(:disabled) {
  background-color: var(--component-button-primary-background-hover);
  border-color: var(--component-button-primary-border-hover);
}

.btn-primary:active:not(:disabled) {
  background-color: var(--component-button-primary-background-active);
  border-color: var(--component-button-primary-border-active);
}

.btn-primary:disabled {
  background-color: var(--component-button-primary-background-disabled);
  color: var(--component-button-primary-text-disabled);
  border-color: var(--component-button-primary-border-disabled);
  cursor: not-allowed;
}

/* セカンダリボタン */
.btn-secondary {
  background-color: var(--component-button-secondary-background-default);
  color: var(--component-button-secondary-text-default);
  border-color: var(--component-button-secondary-border-default);
}

.btn-secondary:hover:not(:disabled) {
  background-color: var(--component-button-secondary-background-hover);
  border-color: var(--component-button-secondary-border-hover);
}

/* ゴーストボタン */
.btn-ghost {
  background-color: var(--component-button-ghost-background-default);
  color: var(--component-button-ghost-text-default);
  border-color: var(--component-button-ghost-border-default);
}

.btn-ghost:hover:not(:disabled) {
  background-color: var(--component-button-ghost-background-hover);
}

/* デンジャーボタン */
.btn-danger {
  background-color: var(--component-button-danger-background-default);
  color: var(--component-button-danger-text-default);
  border-color: var(--component-button-danger-border-default);
}

.btn-danger:hover:not(:disabled) {
  background-color: var(--component-button-danger-background-hover);
  border-color: var(--component-button-danger-border-hover);
}
```

### インプットコンポーネント

```html
<!-- HTML -->
<div class="input-group">
  <label class="input-label" for="email">Email</label>
  <input
    class="input"
    type="email"
    id="email"
    placeholder="your@email.com"
  >
  <span class="input-helper">We'll never share your email.</span>
</div>

<div class="input-group">
  <label class="input-label" for="email-error">Email (Error)</label>
  <input
    class="input input-error"
    type="email"
    id="email-error"
    placeholder="your@email.com"
  >
  <span class="input-helper input-helper-error">Please enter a valid email.</span>
</div>
```

```css
/* CSS */
.input-group {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-2);
}

.input-label {
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-medium);
  color: var(--component-input-label-color-default);
}

.input {
  /* スペーシング */
  padding: var(--spacing-2-5) var(--spacing-4);

  /* タイポグラフィ */
  font-size: var(--font-size-base);
  font-family: var(--font-family-sans);
  line-height: var(--font-line-height-normal);

  /* 背景と色 */
  background-color: var(--component-input-background-default);
  color: var(--component-input-text-default);

  /* ボーダー */
  border-width: var(--border-width-1);
  border-style: solid;
  border-color: var(--component-input-border-default);
  border-radius: var(--border-radius-md);

  /* トランジション */
  transition: border-color var(--animation-duration-fast),
              box-shadow var(--animation-duration-fast);
}

.input::placeholder {
  color: var(--component-input-text-placeholder);
}

.input:hover:not(:disabled) {
  border-color: var(--component-input-border-hover);
}

.input:focus {
  outline: none;
  border-color: var(--component-input-border-focus);
  box-shadow: 0 0 0 var(--component-input-focus-ring-width)
              var(--component-input-focus-ring-color-default);
}

.input:disabled {
  background-color: var(--component-input-background-disabled);
  color: var(--component-input-text-disabled);
  border-color: var(--component-input-border-disabled);
  cursor: not-allowed;
}

/* エラー状態 */
.input-error {
  border-color: var(--component-input-border-error);
}

.input-error:focus {
  border-color: var(--component-input-border-focus-error);
  box-shadow: 0 0 0 var(--component-input-focus-ring-width)
              var(--component-input-focus-ring-color-error);
}

.input-helper {
  font-size: var(--font-size-sm);
  color: var(--component-input-helper-color-default);
}

.input-helper-error {
  color: var(--component-input-helper-color-error);
}
```

### バッジコンポーネント

```html
<!-- HTML -->
<span class="badge badge-success">Success</span>
<span class="badge badge-error">Error</span>
<span class="badge badge-warning">Warning</span>
<span class="badge badge-info">Info</span>
```

```css
/* CSS */
.badge {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-1);

  /* スペーシング */
  padding: var(--spacing-1) var(--spacing-2);

  /* タイポグラフィ */
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-medium);
  line-height: var(--font-line-height-none);

  /* ボーダー */
  border-radius: var(--border-radius-full);
}

.badge-success {
  background-color: var(--component-badge-success-background);
  color: var(--component-badge-success-text);
}

.badge-error {
  background-color: var(--component-badge-error-background);
  color: var(--component-badge-error-text);
}

.badge-warning {
  background-color: var(--component-badge-warning-background);
  color: var(--component-badge-warning-text);
}

.badge-info {
  background-color: var(--component-badge-info-background);
  color: var(--component-badge-info-text);
}
```

---

## カスタムテーマの作成

既存のライト/ダークテーマに加えて、独自のカスタムテーマを作成できます。

### 1. テーマファイルの作成

`src/themes/custom/colors.yaml` を作成：

```yaml
theme:
  custom:
    # プライマリカラー
    primary:
      default:
        value: "{color.purple.500}"
        comment: "カスタムテーマのプライマリカラー"
      hover:
        value: "{color.purple.600}"

    # 背景色
    background:
      primary:
        value: "{color.gray.50}"
      secondary:
        value: "{color.purple.50}"

    # テキスト色
    text:
      primary:
        value: "{color.gray.900}"
      secondary:
        value: "{color.gray.600}"

    # ボーダー色
    border:
      default:
        value: "{color.purple.200}"
```

### 2. ビルド設定の更新

`config.mjs` を確認し、テーマファイルが含まれていることを確認：

```javascript
source: [
  'src/base/**/*.yaml',
  'src/semantic/**/*.yaml',
  'src/components/**/*.yaml',
  'src/themes/**/*.yaml'  // ← custom/colors.yaml も含まれる
]
```

### 3. ビルドして使用

```bash
pnpm build
```

```html
<html data-theme="custom">
  <!-- カスタムテーマが適用される -->
</html>
```

---

## ベストプラクティス

### 1. コンポーネントトークンを優先

✅ **推奨**: コンポーネント専用トークンを使用
```css
.button {
  background-color: var(--component-button-primary-background-default);
}
```

⚠️ **注意**: セマンティックトークンは汎用的な場合のみ
```css
.custom-element {
  background-color: var(--primary-default);
}
```

❌ **非推奨**: ベーストークンの直接使用は避ける
```css
.element {
  background-color: var(--color-blue-500); /* テーマ切り替えに対応しない */
}
```

### 2. テーマに依存しない値

アニメーション時間やスペーシングなど、テーマに依存しない値は、ベーストークンを直接使用してOKです。

```css
.element {
  /* これらはテーマ依存しないのでOK */
  padding: var(--spacing-4);
  border-radius: var(--border-radius-md);
  transition-duration: var(--animation-duration-fast);
}
```

### 3. フォールバック値の設定

古いブラウザ対応が必要な場合、フォールバック値を設定します。

```css
.element {
  /* CSS変数未対応ブラウザ用のフォールバック */
  background-color: #3b82f6;
  background-color: var(--primary-default, #3b82f6);
}
```

### 4. パフォーマンス最適化

不要なCSS変数の再計算を避けるため、`:root` レベルでの変数定義を推奨します。

```css
/* 良い例 */
:root {
  --my-custom-color: var(--primary-default);
}

.element {
  background-color: var(--my-custom-color);
}

/* 避けるべき例（毎回再計算） */
.element {
  --my-custom-color: var(--primary-default);
  background-color: var(--my-custom-color);
}
```

---

## トラブルシューティング

### Q: CSS変数が適用されない

**A**: 以下を確認してください：

1. CSSファイルが正しく読み込まれているか
   ```html
   <link rel="stylesheet" href="path/to/variables.css">
   ```

2. ブラウザの開発者ツールでCSS変数の値を確認
   ```
   Computed > Filter: --component
   ```

3. CSS変数の名前にタイポがないか確認

### Q: テーマ切り替えが反映されない

**A**: 以下を確認してください：

1. `data-theme` 属性が `<html>` タグに設定されているか
   ```javascript
   console.log(document.documentElement.getAttribute('data-theme'));
   ```

2. テーマ定義が正しくビルドされているか
   ```bash
   pnpm build
   ```

3. ブラウザのキャッシュをクリア

### Q: TypeScript型定義が効かない

**A**: 以下を確認してください：

1. パッケージが正しくインストールされているか
2. `tsconfig.json` で型定義ファイルが認識されているか
3. IDEを再起動

---

## まとめ

このガイドでは、Hidearea Design System Tokensの基本的な使い方から、カスタムテーマの作成まで説明しました。

**次のステップ**:
- [アーキテクチャガイド](./アーキテクチャガイド.md) で内部構造を理解
- [コンポーネントリファレンス](./components/) で利用可能なトークンを確認
- [移行ガイド](./移行ガイド.md) で既存プロジェクトへの導入方法を学ぶ

質問や問題がある場合は、[GitHubのIssue](https://github.com/hardlitchi/hidearea-design/issues)で報告してください。
