# 移行ガイド

**Hidearea Design System Tokens**
**最終更新:** 2025-12-10
**対象:** Phase 3 完了版への移行

---

## 概要

このガイドでは、既存のプロジェクトをHidearea Design System Tokens Phase 3完了版に移行する手順を説明します。Phase 3では二層変数アーキテクチャが確立され、全9コンポーネントが機能別カテゴリに再編成されました。

---

## 主要な変更点

### 1. ディレクトリ構造の変更

**旧構造** (Phase 2以前):
```
src/semantic/
└── components.json  # 全コンポーネントが1ファイル
```

**新構造** (Phase 3):
```
src/components/
├── forms/
│   ├── button.yaml
│   └── input.yaml
├── feedback/
│   ├── badge.yaml
│   └── alert.yaml
├── overlays/
│   ├── modal.yaml
│   └── tooltip.yaml
├── data-display/
│   ├── card.yaml
│   └── table.yaml
└── navigation/
    └── navigation.yaml
```

### 2. ファイル形式の統一

- **JSON → YAML**: 全てのトークンファイルがYAML形式に統一
- **コメント機能**: 全トークンに詳細なコメントを追加

### 3. 二層変数システムの導入

**旧方式** (直接参照):
```css
:root {
  --component-button-primary-bg: #3b82f6;
}
```

**新方式** (二層変数):
```css
:root {
  /* コンポーネント層 */
  --component-button-primary-background-default: var(--primary-default);

  /* セマンティック層 */
  --primary-default: var(--theme-light-primary-default, #3b82f6);
}

/* テーマ層 */
[data-theme="light"] {
  --theme-light-primary-default: #3b82f6;
}

[data-theme="dark"] {
  --theme-light-primary-default: #60a5fa;
}
```

---

## 移行手順

### ステップ1: パッケージの更新

```bash
# 最新版にアップデート
pnpm update @hidearea/design-tokens

# または
npm update @hidearea/design-tokens
```

### ステップ2: ビルドファイルの確認

Phase 3のビルド出力を確認します：

```bash
# プロジェクトでトークンをビルド
cd packages/tokens
pnpm build

# 出力ファイルを確認
ls -la build/css/variables.css
ls -la build/js/index.js
```

### ステップ3: トークン名の更新

#### 変更されたトークン名

多くのトークン名が体系化されました。以下の対応表を参照してください：

**Background (背景) トークン:**

| 旧トークン名 | 新トークン名 |
|------------|------------|
| `--component-button-primary-bg` | `--component-button-primary-background-default` |
| `--component-button-primary-bg-hover` | `--component-button-primary-background-hover` |
| `--component-card-bg` | `--component-card-background-default` |

**Text (テキスト) トークン:**

| 旧トークン名 | 新トークン名 |
|------------|------------|
| `--component-button-primary-color` | `--component-button-primary-text-default` |
| `--component-input-color` | `--component-input-text-default` |

**Border (ボーダー) トークン:**

| 旧トークン名 | 新トークン名 |
|------------|------------|
| `--component-button-primary-border` | `--component-button-primary-border-default` |
| `--component-input-border` | `--component-input-border-default` |

#### 一括置換スクリプト

プロジェクト全体のトークン名を一括更新するスクリプト：

```bash
#!/bin/bash
# migrate-tokens.sh

# Background トークンの更新
find . -type f \( -name "*.css" -o -name "*.scss" -o -name "*.tsx" -o -name "*.jsx" \) \
  -exec sed -i 's/--component-\([a-z-]*\)-bg\([^-]\)/--component-\1-background-default\2/g' {} +

# Text トークンの更新
find . -type f \( -name "*.css" -o -name "*.scss" -o -name "*.tsx" -o -name "*.jsx" \) \
  -exec sed -i 's/--component-\([a-z-]*\)-color\([^-]\)/--component-\1-text-default\2/g' {} +

# Border トークンの更新
find . -type f \( -name "*.css" -o -name "*.scss" -o -name "*.tsx" -o -name "*.jsx" \) \
  -exec sed -i 's/--component-\([a-z-]*\)-border\([^-]\)/--component-\1-border-default\2/g' {} +

echo "トークン名の更新が完了しました"
```

### ステップ4: テーマシステムの導入

#### 4-1. HTMLにdata-theme属性を追加

```html
<!DOCTYPE html>
<html data-theme="light" lang="ja">
<head>
  <link rel="stylesheet" href="path/to/variables.css">
</head>
<body>
  <!-- コンテンツ -->
</body>
</html>
```

#### 4-2. テーマ切り替え機能の実装

```javascript
// theme.js
function initTheme() {
  // 保存されたテーマを読み込み、なければシステム設定を使用
  const savedTheme = localStorage.getItem('theme');
  const systemTheme = window.matchMedia('(prefers-color-scheme: dark)').matches
    ? 'dark'
    : 'light';
  const theme = savedTheme || systemTheme;

  document.documentElement.setAttribute('data-theme', theme);
}

function setTheme(theme) {
  document.documentElement.setAttribute('data-theme', theme);
  localStorage.setItem('theme', theme);
}

// ページ読み込み時に初期化
document.addEventListener('DOMContentLoaded', initTheme);
```

#### 4-3. テーマトグルボタンの追加

```html
<button id="theme-toggle" aria-label="テーマ切り替え">
  <span class="light-icon">☀️</span>
  <span class="dark-icon">🌙</span>
</button>

<script>
document.getElementById('theme-toggle').addEventListener('click', () => {
  const current = document.documentElement.getAttribute('data-theme');
  const next = current === 'light' ? 'dark' : 'light';
  setTheme(next);
});
</script>
```

### ステップ5: コンポーネントスタイルの更新

#### ボタンコンポーネント

**旧スタイル:**
```css
.button-primary {
  background: var(--component-button-primary-bg);
  color: var(--component-button-primary-color);
  border: 1px solid var(--component-button-primary-border);
}

.button-primary:hover {
  background: var(--component-button-primary-bg-hover);
}
```

**新スタイル:**
```css
.button-primary {
  background-color: var(--component-button-primary-background-default);
  color: var(--component-button-primary-text-default);
  border-color: var(--component-button-primary-border-default);
}

.button-primary:hover:not(:disabled) {
  background-color: var(--component-button-primary-background-hover);
  border-color: var(--component-button-primary-border-hover);
}

.button-primary:active:not(:disabled) {
  background-color: var(--component-button-primary-background-active);
  border-color: var(--component-button-primary-border-active);
}

.button-primary:disabled {
  background-color: var(--component-button-primary-background-disabled);
  color: var(--component-button-primary-text-disabled);
  border-color: var(--component-button-primary-border-disabled);
  cursor: not-allowed;
}
```

---

## 破壊的変更

### 1. トークン名の体系化

全てのトークン名が以下のパターンに統一されました：

```
component.{component}.{variant?}.{property}.{state}
```

**影響:**
- 既存のCSS/JSコードでトークン名を更新する必要があります
- 上記の一括置換スクリプトで大部分は自動更新可能

### 2. JSONからYAMLへの変更

トークン定義ファイルがJSONからYAMLに変更されました。

**影響:**
- カスタムトークンを追加している場合、YAML形式に変換が必要
- ビルド出力（CSS/JS）には影響なし

### 3. neutralトークンの削除

`color.neutral.*` トークンが削除され、`color.gray.*` に統合されました。

**移行:**
```diff
- var(--color-neutral-500)
+ var(--color-gray-500)
```

---

## チェックリスト

移行作業の完了を確認するためのチェックリスト：

### 基本設定

- [ ] パッケージを最新版に更新
- [ ] トークンのビルドが成功
- [ ] ビルドファイル（CSS/JS）が生成される

### トークン名の更新

- [ ] Backgroundトークンを `*-background-default` に更新
- [ ] Textトークンを `*-text-default` に更新
- [ ] Borderトークンを `*-border-default` に更新
- [ ] 状態トークン（hover, active, disabled）を追加

### テーマシステム

- [ ] HTMLに `data-theme` 属性を追加
- [ ] テーマ初期化スクリプトを実装
- [ ] テーマ切り替え機能を実装
- [ ] システム設定への対応を実装

### コンポーネント

- [ ] Button コンポーネントを更新
- [ ] Input コンポーネントを更新
- [ ] Badge コンポーネントを更新
- [ ] Alert コンポーネントを更新
- [ ] その他のコンポーネントを更新

### 動作確認

- [ ] ライトテーマで正しく表示される
- [ ] ダークテーマで正しく表示される
- [ ] テーマ切り替えが動作する
- [ ] 全ての状態（hover, active, disabled）が正しく表示される
- [ ] ビルドサイズが許容範囲内

---

## トラブルシューティング

### Q: トークンが見つからないエラー

**エラー例:**
```
Error: Token not found: {primary.default}
```

**解決方法:**
1. `src/semantic/aliases.yaml` に該当トークンが存在するか確認
2. トークン名にタイポがないか確認
3. ビルドを再実行: `pnpm build`

### Q: テーマが切り替わらない

**症状:**
`data-theme` を変更してもスタイルが変わらない

**解決方法:**
1. ブラウザの開発者ツールで `data-theme` 属性を確認
2. CSS変数の値を確認（Computed > Filter: `--theme`）
3. セレクタ `[data-theme="dark"]` が正しく適用されているか確認
4. ブラウザのキャッシュをクリア

### Q: ビルドが失敗する

**エラー例:**
```
Error: Invalid YAML syntax
```

**解決方法:**
1. YAMLファイルのインデントを確認（スペース2個）
2. コロンの後にスペースがあるか確認
3. 文字列に特殊文字がある場合はクォートで囲む
4. オンラインYAMLバリデーターで構文チェック

### Q: TypeScript型定義が効かない

**症状:**
トークンのオートコンプリートが表示されない

**解決方法:**
1. `build/ts/index.d.ts` が生成されているか確認
2. `tsconfig.json` で型定義ファイルが認識されているか確認
3. IDEを再起動
4. `node_modules` を削除して再インストール

---

## パフォーマンス最適化

### バンドルサイズの確認

```bash
# バンドルサイズを測定
pnpm measure:size
```

**Phase 3の目標値:**
- `js/index.js`: 55 KB raw / 15 KB gzip
- `css/variables.css`: 100 KB raw / 20 KB gzip

### 最適化のヒント

1. **使用しないトークンの削除**
   ```javascript
   // tree-shakingを有効化
   import { button, input } from '@hidearea/design-tokens/components';
   ```

2. **CSS変数のスコープ最適化**
   ```css
   /* :root レベルで定義（推奨） */
   :root {
     --custom-var: var(--primary-default);
   }

   /* 要素レベルでの定義は避ける（パフォーマンス低下） */
   .element {
     --custom-var: var(--primary-default); /* 避ける */
   }
   ```

3. **Critical CSSの抽出**
   初回レンダリングに必要な最小限のトークンのみをインライン化

---

## 段階的な移行戦略

大規模プロジェクトの場合、段階的な移行を推奨します：

### フェーズ1: 準備 (1-2日)

1. 最新パッケージのインストール
2. テーマシステムの実装
3. 開発環境でのテスト

### フェーズ2: コンポーネント移行 (1週間)

1. 最も使用頻度の高いコンポーネントから移行
   - Button
   - Input
   - Card

2. 中程度のコンポーネント
   - Badge
   - Alert
   - Modal

3. 低頻度のコンポーネント
   - Tooltip
   - Table
   - Navigation

### フェーズ3: 全体テスト (2-3日)

1. 全ページでの表示確認
2. テーマ切り替えの動作確認
3. パフォーマンステスト
4. クロスブラウザテスト

### フェーズ4: 本番デプロイ (1日)

1. ステージング環境でのテスト
2. 本番デプロイ
3. モニタリング

---

## サポートとフィードバック

移行で問題が発生した場合：

1. **ドキュメントを確認**
   - [アーキテクチャガイド](./アーキテクチャガイド.md)
   - [使用方法ガイド](./使用方法ガイド.md)
   - [コンポーネントリファレンス](./components/)

2. **GitHubで報告**
   - [Issue を作成](https://github.com/hardlitchi/hidearea-design/issues)
   - バグレポート、機能リクエスト、質問を受け付けています

3. **コミュニティに質問**
   - Discussions で他の開発者と情報交換

---

## まとめ

Phase 3への移行により、以下のメリットが得られます：

✅ **動的テーマ切り替え** - ランタイムでのテーマ変更が可能
✅ **体系的なトークン名** - 一貫性のある命名規則
✅ **保守性の向上** - 機能別カテゴリによる整理
✅ **開発体験の向上** - TypeScript型定義、詳細なドキュメント

移行作業は段階的に進め、十分なテストを行うことを推奨します。

---

**関連ドキュメント:**
- [アーキテクチャガイド](./アーキテクチャガイド.md)
- [使用方法ガイド](./使用方法ガイド.md)
- [コンポーネントリファレンス](./components/README.md)
